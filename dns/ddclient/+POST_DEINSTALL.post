#!/bin/sh

# fall back to the native backend, the ddclient binary may remain installed
configctl template reload OPNsense/ddclient > /dev/null 2>&1 || :
configctl ddclient restart > /dev/null 2>&1 || :
