The Beep Beep plugin allows users to choose custom startup and
shutdown beep sounds through the OPNsense web interface. Users can
select from a list of predefined beep tunes that play through the
PC motherboard on boot and shutdown events.

The beep tunes were created and provided by the OPNsense user fctr

WWW: https://github.com/opnsense/plugins
